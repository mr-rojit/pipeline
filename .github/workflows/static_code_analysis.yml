name: run-static-code-analysis
on: push
jobs:
  format-and-lint:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: list-dir
        run: ls -la
        